# Example environment variables for NestJS project

# MongoDB connection string (replace with your real credentials locally)
MONGO_URI=your_mongodb_connection_uri_here

# JWT secret key (replace with a secure string locally)
JWT_SECRET=your_jwt_secret_here

# Optional: server port
PORT=4000
